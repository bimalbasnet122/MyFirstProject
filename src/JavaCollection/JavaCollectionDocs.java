package JavaCollection;

public class JavaCollectionDocs {
	
	/*
	 * --------Collection framework / Java Collection / DS
	 * 
	 * =>use to store and processing dynamic multiple data/objects.
	 * 
	 * 1.List: dynamic array 
	 * 		a. ArrayList: Slow processing, better to use for data storage
	 * 		b. LinkedList: Fast processing, better to use for data processing
	 * 
	 * 2.Set: collection of unique data
	 * 		a. HashSet: Unique data only
	 * 		b. TreeSet: Unique + sorted data(1 2 3  4 ...............)
	 * 		c. LinkedHashSet: Unique + insertion in order(jun order ma rakhya tei hisab le aaune0
	 * 
	 * 3.Map: data manage in key, value pair
	 * 		a. HashMap: <k,v> pair
	 * 		b. TreeMap: <k,v> pair + sorted data
	 * 		c. LinkedHashMap: <k,v> pair + insertion in order
	 * 
	 */

}
