package Array;

public class ArrayDocs {
	
	
	/*
	 * ----------------Array--------------------
	 * use to store and processing static multiple data
	 * collection of homogeneous data
	 * group of multiple values of same data type with fixed size
	 * data stored on the basis of indices (index starts from 0)
	 * contains duplicate data 
	 * random access 
	 * contiguous memory allocation(sangai jodiyera basera ko memory)chain
	 * array itself is an object in java
	 * 
	 * 	syntax:
	 * 		data_type array_name[] = new data_type[size];
	 * 		
	 * 		eg
	 * 			String names[]=new String [200];
	 * 			char[] alpha[]= new cbar[26];
	 * 
	 * 	 */

}
